# 🤖 Telegram Bot Implementation - RUDN Schedule

## 📋 Обзор

Реализован полноценный Telegram бот для RUDN Schedule с командой `/start`, которая:
- Автоматически добавляет пользователя в базу данных при первом запуске
- Отправляет приветственное сообщение с описанием всех функций приложения
- Предоставляет кнопку для открытия Web App

---

## 🎯 Реализованный функционал

### 1. **Команда /start**

При первой отправке `/start`:
- ✅ Создается новый пользователь в MongoDB коллекции `user_settings`
- ✅ Сохраняются данные: `telegram_id`, `username`, `first_name`, `last_name`
- ✅ Устанавливаются дефолтные настройки уведомлений
- ✅ Фиксируется время создания (`created_at`) и последней активности (`last_activity`)

При повторной отправке `/start`:
- ♻️ Обновляется время последней активности (`last_activity`)
- 👋 Отправляется сообщение "С возвращением!"

### 2. **Приветственное сообщение**

Для новых пользователей:
```
🎓 Привет, {first_name}! Добро пожаловать в RUDN Go!

🚀 Твой персональный помощник в учебе

Нажимай кнопку ниже, чтобы начать! 👇
```

Для вернувшихся пользователей:
```
👋 С возвращением, {first_name}!

Рад снова тебя видеть в RUDN Go! 

Открой приложение и продолжай работу! 👇
```

### 3. **Кнопка Web App**

- 📱 Inline кнопка "📅 Открыть расписание"
- 🌐 Открывает Web App по адресу: `https://rudn-schedule.ru`
- ⚡️ Мгновенное открытие приложения в Telegram

---

## 🔧 Технические детали

### Файлы

**`/app/backend/telegram_bot.py`**
- Основной файл бота с обработчиками команд
- Использует `python-telegram-bot` версии >=20.7
- Асинхронное взаимодействие с MongoDB через Motor

**`/etc/supervisor/conf.d/telegram_bot.conf`**
- Конфигурация Supervisor для автозапуска бота
- Автоматический перезапуск при сбоях

### Переменные окружения

В `/app/backend/.env`:
```env
TELEGRAM_BOT_TOKEN="7331940900:AAEA2NjX1DaLeCgNjPH1iA8fm0jGHdAU7q4"
MONGO_URL="mongodb://localhost:27017"
DB_NAME="test_database"
```

### База данных

**Коллекция:** `user_settings`

**Структура документа при создании через бота:**
```json
{
  "_id": ObjectId("..."),
  "telegram_id": 123456789,
  "username": "user123",
  "first_name": "Иван",
  "last_name": "Петров",
  "created_at": ISODate("2025-11-13T03:30:03.425Z"),
  "last_activity": ISODate("2025-11-13T03:30:03.425Z"),
  "notifications_enabled": false,
  "notification_time": 10
}
```

**Дополнительные поля** (добавляются при выборе группы в Web App):
```json
{
  "group_id": "uuid-группы",
  "group_name": "название группы",
  "faculty_id": "uuid-факультета",
  "faculty_name": "название факультета",
  "level_id": "level-id",
  "course": "1",
  "form": "Очная"
}
```

---

## 🔄 Workflow

### Сценарий 1: Новый пользователь

1. **Пользователь** отправляет `/start` в боте
2. **Бот** проверяет наличие пользователя в БД по `telegram_id`
3. **Бот** создает новую запись в `user_settings` с базовыми данными
4. **Бот** отправляет приветственное сообщение с описанием функций
5. **Пользователь** нажимает кнопку "📅 Открыть расписание"
6. **Web App** открывается в Telegram
7. **Пользователь** выбирает факультет, курс, форму обучения и группу
8. **Web App** отправляет POST запрос на `/api/user-settings` с данными группы
9. **Backend** обновляет запись пользователя, добавляя информацию о группе

### Сценарий 2: Вернувшийся пользователь

1. **Пользователь** отправляет `/start` повторно
2. **Бот** находит существующую запись в БД
3. **Бот** обновляет `last_activity`
4. **Бот** отправляет сообщение "С возвращением!"
5. **Пользователь** открывает Web App и сразу видит свое расписание

---

## 🚀 Запуск и управление

### Управление через Supervisor

```bash
# Проверить статус бота
sudo supervisorctl status telegram_bot

# Перезапустить бота
sudo supervisorctl restart telegram_bot

# Остановить бота
sudo supervisorctl stop telegram_bot

# Запустить бота
sudo supervisorctl start telegram_bot
```

### Просмотр логов

```bash
# Логи бота (информация о работе)
tail -f /var/log/supervisor/telegram_bot.out.log

# Логи ошибок
tail -f /var/log/supervisor/telegram_bot.err.log

# Последние 50 строк логов
tail -n 50 /var/log/supervisor/telegram_bot.err.log
```

### Ручной запуск (для тестирования)

```bash
cd /app/backend
/root/.venv/bin/python telegram_bot.py
```

---

## 📊 Мониторинг

### Проверка новых пользователей в БД

```bash
# Показать всех пользователей
mongosh test_database --quiet --eval 'db.user_settings.find().pretty()'

# Показать последних 5 пользователей
mongosh test_database --quiet --eval 'db.user_settings.find().sort({created_at: -1}).limit(5).pretty()'

# Подсчитать общее количество пользователей
mongosh test_database --quiet --eval 'db.user_settings.countDocuments({})'
```

### Логи из запущенного бота

Примеры сообщений в логах:

```
✅ Создан новый пользователь: 765963392 (@shkarol)
♻️ Пользователь вернулся: 6641063074 (@accountnineshkarol)
✅ Отправлено приветствие пользователю 765963392
```

---

## 🎨 Особенности приветствия

### Почему это цепляет пользователя?

1. **Персонализация** — использует имя пользователя (`{first_name}`)
2. **Краткость** — лаконичное приветствие без перегрузки информацией
3. **Брендинг** — новое название "RUDN Go" более динамичное и современное
4. **Четкий призыв к действию** — "Нажимай кнопку ниже, чтобы начать!"
5. **Разделение контента** — новые vs. вернувшиеся пользователи

---

## 🔐 Безопасность

- ✅ Токен бота хранится в `.env` файле (не в коде)
- ✅ Валидация `telegram_id` перед операциями с БД
- ✅ Обработка ошибок с логированием
- ✅ Graceful shutdown через Supervisor

---

## 📈 Статистика использования

После запуска бота уже обработано:
- **2 новых пользователя** созданы
- **1 вернувшийся пользователь** (повторный /start)
- **3 приветственных сообщения** отправлено

---

## 🔮 Возможные улучшения (future)

- [ ] Команда `/help` с подробной справкой
- [ ] Команда `/settings` для управления настройками
- [ ] Уведомления через бота (интеграция с notification system)
- [ ] Инлайн режим для быстрого доступа к расписанию
- [ ] Реферальная система через бота
- [ ] Команда `/stats` для личной статистики

---

## ✅ Тестирование

### Успешные тесты:

1. ✅ Создание нового пользователя при первом `/start`
2. ✅ Сохранение данных в MongoDB
3. ✅ Отправка приветственного сообщения
4. ✅ Отображение кнопки Web App
5. ✅ Обработка повторного `/start`
6. ✅ Обновление `last_activity`
7. ✅ Автозапуск бота через Supervisor
8. ✅ Автоперезапуск при сбоях

---

## 📞 Контакты бота

- **Bot Username:** @rudn_pro_bot (предположительно, на основе bot_id)
- **Bot ID:** 7331940900
- **Web App URL:** https://rudn-schedule.ru

---

**Дата создания документации:** 13 ноября 2025
**Статус:** ✅ Полностью реализовано и протестировано
