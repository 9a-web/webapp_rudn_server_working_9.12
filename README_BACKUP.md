# 🗄️ Полное руководство по бэкапу MongoDB для RUDN Schedule App

## 📋 Содержание
1. [Быстрый старт](#быстрый-старт)
2. [Способ 1: API экспорт (рекомендуется)](#способ-1-api-экспорт-рекомендуется)
3. [Способ 2: MongoDB бинарный бэкап](#способ-2-mongodb-бинарный-бэкап)
4. [Способ 3: JSON экспорт коллекций](#способ-3-json-экспорт-коллекций)
5. [Восстановление данных](#восстановление-данных)
6. [Скачивание на локальный компьютер](#скачивание-на-локальный-компьютер)

---

## ⚡ Быстрый старт

### Самый простой способ (API):
```bash
./download_backup.sh
```

### Полный бэкап (MongoDB + JSON + архив):
```bash
./backup_mongodb.sh
```

### Быстрый JSON экспорт:
```bash
./export_json.sh
```

---

## 📊 Информация о базе данных

**База данных:** `rudn_schedule` (MongoDB, localhost:27017)

**Коллекции (7 шт):**
- `user_settings` - настройки пользователей
- `user_stats` - статистика активности
- `user_achievements` - достижения
- `tasks` - личные задачи
- `rooms` - комнаты для групповой работы
- `room_participants` - участники комнат
- `group_tasks` - групповые задачи

---

## Способ 1: API экспорт (рекомендуется)

### ✅ Преимущества:
- Простой и быстрый
- Не требует прямого доступа к MongoDB
- JSON формат (легко читать и импортировать)
- Работает через REST API

### 🚀 Использование:

**1. Автоматический экспорт (один скрипт):**
```bash
./download_backup.sh
```

**2. Ручной экспорт через curl:**

Статистика базы:
```bash
curl -s "http://localhost:8001/api/backup/stats" | python3 -m json.tool
```

Полный экспорт:
```bash
curl -s "http://localhost:8001/api/export/database" > backup_$(date +%Y%m%d).json
```

Экспорт отдельной коллекции:
```bash
curl -s "http://localhost:8001/api/export/collection/user_settings" > user_settings.json
curl -s "http://localhost:8001/api/export/collection/tasks" > tasks.json
curl -s "http://localhost:8001/api/export/collection/rooms" > rooms.json
```

### 📁 Результат:
- Файлы сохраняются в `/app/api_backups/`
- Формат: `database_backup_YYYYMMDD_HHMMSS.json`
- Содержит все коллекции и метаданные

---

## Способ 2: MongoDB бинарный бэкап

### ✅ Преимущества:
- Полный нативный бэкап MongoDB
- Быстрое восстановление через `mongorestore`
- Поддержка инкрементных бэкапов
- Архивирование в tar.gz

### 🚀 Использование:

```bash
./backup_mongodb.sh
```

### 📁 Что создается:
```
/app/backups/
├── rudn_schedule_backup_20250122_150000.tar.gz  ← Архив (для скачивания)
├── rudn_schedule_backup_20250122_150000_binary/ ← Бинарный бэкап
│   └── rudn_schedule/
│       ├── user_settings.bson
│       ├── user_stats.bson
│       ├── tasks.bson
│       └── ... (все коллекции)
└── rudn_schedule_backup_20250122_150000_json/   ← JSON файлы
    ├── user_settings.json
    ├── user_stats.json
    └── ... (все коллекции)
```

### 🔍 Особенности скрипта:
- Проверка подключения к MongoDB
- Показ статистики базы данных
- Создание бинарного бэкапа (`mongodump`)
- Создание JSON копии (`mongoexport`)
- Автоматическое архивирование
- Детальные логи процесса

---

## Способ 3: JSON экспорт коллекций

### ✅ Преимущества:
- Самый быстрый способ
- Читаемый формат JSON
- Легко редактировать вручную
- Подходит для миграций

### 🚀 Использование:

```bash
./export_json.sh
```

### 📁 Результат:
```
/app/exports/
└── export_20250122_150000/
    ├── user_settings.json
    ├── user_stats.json
    ├── user_achievements.json
    ├── tasks.json
    ├── rooms.json
    ├── room_participants.json
    └── group_tasks.json
```

---

## 🔄 Восстановление данных

### Из бинарного бэкапа:

```bash
./restore_mongodb.sh /app/backups/rudn_schedule_backup_20250122_150000_binary/rudn_schedule
```

⚠️ **Внимание:** Это удалит текущие данные и заменит их из бэкапа!

### Из архива:

```bash
# 1. Распаковать архив
cd /app/backups
tar -xzf rudn_schedule_backup_20250122_150000.tar.gz

# 2. Восстановить
./restore_mongodb.sh /app/backups/rudn_schedule_backup_20250122_150000_binary/rudn_schedule
```

### Импорт отдельной коллекции:

```bash
mongoimport --uri="mongodb://localhost:27017" \
    --db=rudn_schedule \
    --collection=user_settings \
    --file=/app/exports/export_20250122_150000/user_settings.json \
    --jsonArray
```

---

## 📥 Скачивание на локальный компьютер

### Способ 1: Base64 (для любых размеров)

**На сервере:**
```bash
# Создать бэкап
./download_backup.sh

# Закодировать файл
cat /app/api_backups/database_backup_20251122_200355.json | base64 > backup_encoded.txt

# Вывести на экран (скопировать)
cat backup_encoded.txt
```

**На локальном компьютере (Linux/Mac):**
```bash
# Вставить base64 текст в файл backup_encoded.txt, затем:
cat backup_encoded.txt | base64 -d > rudn_schedule_backup.json
```

**На Windows (PowerShell):**
```powershell
$base64 = Get-Content backup_encoded.txt
[System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($base64)) | Set-Content rudn_schedule_backup.json
```

### Способ 2: Архив tar.gz

**На сервере:**
```bash
# Создать полный бэкап
./backup_mongodb.sh

# Закодировать архив
cat /app/backups/rudn_schedule_backup_20250122_150000.tar.gz | base64 > backup.txt
```

**На локальном компьютере:**
```bash
# Linux/Mac
cat backup.txt | base64 -d > backup.tar.gz
tar -xzf backup.tar.gz

# Windows
# Используйте 7-Zip или WinRAR для распаковки .tar.gz
```

### Способ 3: Прямое скачивание (если есть SSH/FTP)

```bash
# SCP (если есть SSH доступ)
scp user@server:/app/api_backups/database_backup_*.json ./

# SFTP
sftp user@server
get /app/api_backups/database_backup_*.json
```

---

## 🛠️ Полезные команды

### Проверка базы данных

**Статистика:**
```bash
mongosh mongodb://localhost:27017/rudn_schedule --eval "db.stats()"
```

**Список коллекций:**
```bash
mongosh mongodb://localhost:27017/rudn_schedule --eval "db.getCollectionNames()"
```

**Количество документов:**
```bash
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  db.getCollectionNames().forEach(function(col) {
    print(col + ': ' + db[col].countDocuments());
  })
"
```

**Размер базы данных:**
```bash
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  var stats = db.stats();
  print('Размер данных: ' + (stats.dataSize / 1024 / 1024).toFixed(2) + ' MB');
  print('Размер на диске: ' + (stats.storageSize / 1024 / 1024).toFixed(2) + ' MB');
"
```

### Управление бэкапами

**Список всех бэкапов:**
```bash
ls -lh /app/backups/
ls -lh /app/api_backups/
ls -lh /app/exports/
```

**Удаление старых бэкапов (старше 7 дней):**
```bash
find /app/backups/ -name "*.tar.gz" -mtime +7 -delete
find /app/api_backups/ -name "*.json" -mtime +7 -delete
```

**Размер всех бэкапов:**
```bash
du -sh /app/backups /app/api_backups /app/exports
```

---

## ⏰ Автоматизация бэкапов

### Создать cron задачу (ежедневный бэкап в 3:00 AM):

```bash
# Открыть crontab
crontab -e

# Добавить строку:
0 3 * * * /app/backup_mongodb.sh >> /var/log/mongodb_backup.log 2>&1
```

### Создать скрипт автоочистки старых бэкапов:

```bash
#!/bin/bash
# /app/cleanup_old_backups.sh

# Удалить бэкапы старше 30 дней
find /app/backups/ -name "*.tar.gz" -mtime +30 -delete
find /app/api_backups/ -name "*.json" -mtime +30 -delete
find /app/exports/ -type d -mtime +30 -exec rm -rf {} +

echo "Старые бэкапы удалены: $(date)"
```

---

## 🔒 Безопасность

1. **Храните бэкапы в безопасном месте** - скачивайте на локальный компьютер
2. **Шифруйте чувствительные данные** - используйте GPG или аналоги
3. **Регулярно тестируйте восстановление** - убедитесь, что бэкапы рабочие
4. **Не публикуйте бэкапы в открытый доступ** - содержат персональные данные

---

## ❓ Решение проблем

### "permission denied"
```bash
chmod +x /app/*.sh
```

### "command not found: mongodump"
MongoDB tools уже установлены, но если возникла ошибка:
```bash
which mongodump mongoexport mongoimport
```

### "connection refused"
Проверьте, что MongoDB запущен:
```bash
sudo supervisorctl status
mongosh mongodb://localhost:27017 --eval "db.version()"
```

### База данных пустая
Это нормально для нового приложения. Бэкап можно создать в любой момент.

---

## 📈 Рекомендации

✅ **DO:**
- Создавайте бэкап перед важными обновлениями
- Храните минимум 3 последних бэкапа
- Тестируйте восстановление раз в месяц
- Скачивайте важные бэкапы на локальный компьютер
- Используйте API экспорт для быстрых бэкапов
- Используйте mongodump для полных бэкапов

❌ **DON'T:**
- Не храните все бэкапы вечно (занимают место)
- Не публикуйте бэкапы в открытый доступ
- Не забывайте проверять, что бэкап создался успешно

---

## 📞 Контакты

**Проект:** RUDN Schedule Telegram Web App  
**База данных:** MongoDB (rudn_schedule)  
**Создано:** Emergent AI Agent  
**Дата:** 2025-11-22

---

**Все готово! Выберите удобный способ бэкапа и защитите свои данные! 🛡️**
