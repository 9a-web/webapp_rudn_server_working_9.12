# 🐛 Исправление: Автоматическая установка дедлайна при создании задачи

## Дата: 6 ноября 2024
## Версия: v2.0

---

## ❌ ПРОБЛЕМА

При создании задачи за прошедший день (или любой другой день, кроме сегодняшнего) в селекторе дат, задача создавалась без дедлайна или с неправильной датой.

### Пример проблемы:
1. Пользователь выбирает вчерашнюю дату в WeekDateSelector
2. Нажимает "+" для создания новой задачи
3. Открывается AddTaskModal
4. Дедлайн НЕ устанавливается автоматически (остаётся пустым)
5. Пользователь вводит текст задачи и нажимает "Создать"
6. Задача создаётся БЕЗ дедлайна
7. Задача не отображается на выбранной дате (потому что нет дедлайна)

---

## ✅ РЕШЕНИЕ

Убрана условная проверка. Теперь дедлайн **ВСЕГДА** устанавливается на выбранную дату при открытии модального окна:

```javascript
// Новая логика (ИСПРАВЛЕННАЯ)
useEffect(() => {
  if (isOpen && selectedDate) {
    // ВСЕГДА устанавливаем дедлайн на выбранную дату
    const deadlineDate = new Date(selectedDate);
    deadlineDate.setHours(23, 59, 0, 0);
    const formattedDeadline = deadlineDate.toISOString().slice(0, 16);
    setDeadline(formattedDeadline);
  }
}, [isOpen, selectedDate]);
```

---

## 🎯 РЕЗУЛЬТАТ

### После исправления:
- ✅ Дедлайн всегда устанавливается на выбранную дату
- ✅ Задачи корректно отображаются на нужной дате
- ✅ Интуитивно понятное поведение для пользователя
- ✅ Пользователь всё ещё может очистить дедлайн вручную при желании

### Файлы изменены:
- ✅ `/app/frontend/src/components/AddTaskModal.jsx`
