# 🚀 Руководство по безопасному обновлению проекта

## ✅ Важно понять

**База данных MongoDB хранится ОТДЕЛЬНО от кода приложения!**

- Код (frontend/backend) находится в файлах проекта
- База данных находится в `/var/lib/mongodb/` (отдельное хранилище)
- **При обновлении кода данные НЕ удаляются автоматически**

Но всё равно **ОБЯЗАТЕЛЬНО делайте бэкап** перед любым обновлением!

---

## 🛡️ Безопасное обновление (5 шагов)

### Шаг 1: Создать бэкап текущей БД

**На сервере (серверная версия с данными):**

```bash
# Подключиться к серверу
ssh user@your-server

# Перейти в директорию проекта
cd /app

# Создать бэкап
./download_backup.sh
```

Это создаст файл `/app/api_backups/database_backup_TIMESTAMP.json`

### Шаг 2: Скачать бэкап на локальный компьютер

```bash
# На сервере: закодировать бэкап
cat /app/api_backups/database_backup_*.json | base64 > /tmp/backup_encoded.txt

# Показать содержимое (скопировать)
cat /tmp/backup_encoded.txt
```

**На локальном компьютере:**
```bash
# Сохранить скопированный текст в файл backup.txt, затем:
cat backup.txt | base64 -d > server_backup_$(date +%Y%m%d).json

# Проверить
ls -lh server_backup_*.json
```

✅ **Теперь у вас есть копия данных!**

### Шаг 3: Обновить код на сервере

**Вариант A: Через Git (рекомендуется)**
```bash
# На сервере
cd /app

# Проверить текущую ветку и статус
git status
git branch

# Сохранить локальные изменения (если есть)
git stash

# Обновить код
git pull origin main  # или master, в зависимости от вашей ветки

# Вернуть локальные изменения (если нужно)
git stash pop
```

**Вариант B: Ручное копирование файлов**
- Скопируйте только измененные файлы кода
- НЕ копируйте директорию `/var/lib/mongodb/`
- НЕ трогайте `.env` файлы (если не нужно)

### Шаг 4: Обновить зависимости (если изменились)

```bash
# Backend (если изменился requirements.txt)
cd /app/backend
pip install -r requirements.txt

# Frontend (если изменился package.json)
cd /app/frontend
yarn install
```

### Шаг 5: Перезапустить сервисы

```bash
# Перезапустить всё
sudo supervisorctl restart all

# Проверить статус
sudo supervisorctl status

# Проверить логи
tail -f /var/log/supervisor/backend.out.log
tail -f /var/log/supervisor/frontend.out.log
```

### Шаг 6: Проверить, что всё работает

```bash
# Проверить API
curl http://localhost:8001/api/backup/stats

# Проверить, что данные на месте
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  print('Всего пользователей: ' + db.user_settings.countDocuments());
  print('Всего задач: ' + db.tasks.countDocuments());
  print('Всего комнат: ' + db.rooms.countDocuments());
"
```

---

## 🔄 Процесс обновления на Emergent Platform

Если вы используете платформу Emergent с автоматическим деплоем:

### Через GitHub Integration

1. **Подключить GitHub** (если еще не подключен)
2. **Commit & Push** изменения в ваш репозиторий:
   ```bash
   git add .
   git commit -m "Update: описание изменений"
   git push origin main
   ```
3. **Автоматический деплой** произойдет на Emergent
4. **База данных сохранится** автоматически

### Важно на Emergent:
- БД MongoDB **персистентна** (сохраняется между деплоями)
- Данные хранятся в **Persistent Volume**
- При обновлении кода данные **не удаляются**

---

## 📋 Чеклист перед обновлением

- [ ] Создан бэкап БД на сервере
- [ ] Бэкап скачан на локальный компьютер
- [ ] Проверен список изменений (git diff)
- [ ] Обновлены зависимости (если нужно)
- [ ] Проверены .env файлы (не перезаписать случайно)
- [ ] Запланировано время обновления (лучше в нерабочее время)

---

## ⚠️ Что МОЖЕТ привести к потере данных

❌ **ОПАСНО:**
1. Команды типа `mongosh --eval "db.dropDatabase()"`
2. Удаление директории `/var/lib/mongodb/`
3. Случайное переименование базы данных в .env
4. Запуск `clear_db` команды бота
5. Ручное удаление коллекций в MongoDB

✅ **БЕЗОПАСНО:**
1. Обновление кода через git pull
2. Обновление зависимостей (pip install, yarn install)
3. Перезапуск сервисов (supervisorctl restart)
4. Изменение frontend/backend кода
5. Добавление новых API endpoints

---

## 🔧 Полезные команды для проверки

### Проверить версию кода
```bash
git log --oneline -5  # Последние 5 коммитов
git show HEAD         # Последний коммит
```

### Проверить данные в БД
```bash
# Быстрая статистика
curl http://localhost:8001/api/backup/stats | python3 -m json.tool

# Детальная проверка
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  db.getCollectionNames().forEach(function(col) {
    print(col + ': ' + db[col].countDocuments() + ' documents');
  });
"
```

### Сравнить с локальной версией
```bash
# На сервере
git diff origin/main  # Показать отличия от main ветки
```

---

## 🆘 Если что-то пошло не так

### Вариант 1: Откатить код
```bash
# Посмотреть историю коммитов
git log --oneline

# Откатить на предыдущую версию
git checkout COMMIT_HASH

# Или откатить последний коммит
git reset --hard HEAD~1

# Перезапустить
sudo supervisorctl restart all
```

### Вариант 2: Восстановить данные из бэкапа
```bash
# Если у вас есть локальный бэкап:
./restore_mongodb.sh /app/backups/BACKUP_NAME_binary/rudn_schedule

# Или импортировать JSON
mongoimport --uri="mongodb://localhost:27017" \
  --db=rudn_schedule \
  --collection=user_settings \
  --file=backup.json \
  --jsonArray
```

---

## 📊 Пример полного процесса

```bash
# ========================================
# НА СЕРВЕРЕ (серверная версия с данными)
# ========================================

# 1. Бэкап
cd /app
./download_backup.sh
cat /app/api_backups/database_backup_*.json | base64 > /tmp/server_backup.txt

# 2. Показать статистику ДО обновления
curl http://localhost:8001/api/backup/stats

# 3. Обновить код
git pull origin main

# 4. Обновить зависимости (если нужно)
cd /app/backend && pip install -r requirements.txt
cd /app/frontend && yarn install

# 5. Перезапустить
sudo supervisorctl restart all

# 6. Проверить статус
sleep 5
sudo supervisorctl status

# 7. Проверить данные ПОСЛЕ обновления
curl http://localhost:8001/api/backup/stats

# 8. Сравнить количество документов
mongosh mongodb://localhost:27017/rudn_schedule --eval "
  print('user_settings: ' + db.user_settings.countDocuments());
  print('tasks: ' + db.tasks.countDocuments());
  print('rooms: ' + db.rooms.countDocuments());
"

# ========================================
# НА ЛОКАЛЬНОМ КОМПЬЮТЕРЕ
# ========================================

# Скопировать содержимое /tmp/server_backup.txt и декодировать:
cat server_backup.txt | base64 -d > safe_backup_$(date +%Y%m%d).json
```

---

## 💡 Рекомендации

### ✅ Хорошие практики:
1. **Всегда** делайте бэкап перед обновлением
2. **Храните** минимум 3 последних бэкапа
3. **Тестируйте** обновления на dev/staging окружении
4. **Обновляйте** в нерабочее время (меньше пользователей)
5. **Документируйте** все изменения в git commit messages
6. **Проверяйте** логи после обновления

### ❌ Чего избегать:
1. Не обновляйте без бэкапа
2. Не трогайте .env файлы без необходимости
3. Не запускайте команды удаления БД
4. Не обновляйте в пиковое время
5. Не пропускайте проверку логов

---

## 📞 Дополнительная информация

- **Бэкап:** [BACKUP_GUIDE.md](./BACKUP_GUIDE.md)
- **Шпаргалка:** [BACKUP_CHEATSHEET.md](./BACKUP_CHEATSHEET.md)
- **API:** [README_BACKUP.md](./README_BACKUP.md)

---

**Ваши данные в безопасности! База данных не затрагивается при обновлении кода. Но всегда делайте бэкап - это золотое правило! 🛡️**
