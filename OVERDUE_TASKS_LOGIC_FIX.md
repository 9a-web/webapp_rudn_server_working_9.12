# Исправление логики показа раздела "Просроченные"

## Проблема
Вкладка "Просроченные" появлялась при выборе всех дат в селекторе дней недели, что было логически неверно.

## Анализ проблемы
Функция `groupTasksByDeadline()` группировала задачи относительно **текущего времени (now)**, а не относительно **выбранной даты в селекторе (tasksSelectedDate)**. Это приводило к тому, что просроченные задачи отображались всегда, независимо от того, какую дату выбрал пользователь.

### Старая логика:
```javascript
const now = new Date();
const deadline = new Date(task.deadline);
const diffHours = (deadline - now) / (1000 * 60 * 60);

if (diffHours < 0) {
  overdue.push(task); // Просроченные относительно NOW
}
```

## Решение

### Новая логика группировки задач

Теперь группировка происходит относительно **выбранной даты**:

```javascript
const selectedDateStart = new Date(tasksSelectedDate);
selectedDateStart.setHours(0, 0, 0, 0);

const selectedDateEnd = new Date(tasksSelectedDate);
selectedDateEnd.setHours(23, 59, 59, 999);
```

### Правила группировки:

1. **Просроченные (overdue)**:
   - Дедлайн раньше выбранной даты **И** раньше текущего момента
   - `deadline < selectedDateStart && deadline < now`
   - ✅ Показываются только когда действительно просрочены

2. **На выбранную дату (today)**:
   - Дедлайн в пределах выбранного дня (00:00 - 23:59)
   - `deadline >= selectedDateStart && deadline <= selectedDateEnd`
   - Заголовок меняется динамически: "Сегодня" / "Завтра" / "Вчера" / "На эту дату"

3. **На этой неделе (thisWeek)**:
   - Дедлайн в течение 7 дней от выбранной даты
   - `0 < diffDays <= 7`

4. **Позже (later)**:
   - Дедлайн более чем через 7 дней от выбранной даты
   - `diffDays > 7`

5. **Без срока (noDeadline)**:
   - Задачи без установленного дедлайна

## Дополнительные улучшения

### Динамический заголовок группы
Добавлена функция `getTaskGroupTitle()`, которая определяет заголовок группы в зависимости от выбранной даты:

```javascript
const getTaskGroupTitle = () => {
  const selected = new Date(tasksSelectedDate);
  const today = new Date();
  
  if (selected === today) return 'Сегодня';
  if (selected === today + 1 day) return 'Завтра';
  if (selected === today - 1 day) return 'Вчера';
  
  return 'На эту дату'; // Для любой другой даты
}
```

Теперь в списке задач вместо статичного "Сегодня" отображается:
- "Сегодня" - если выбрана сегодняшняя дата
- "Завтра" - если выбрана завтрашняя дата
- "Вчера" - если выбрана вчерашняя дата
- "На эту дату" - для любой другой даты

## Результаты

### ✅ До исправления:
- Просроченные задачи показывались при выборе любой даты
- Группировка была относительно текущего момента
- Заголовок группы всегда был "Сегодня"

### ✅ После исправления:
- Просроченные задачи показываются только когда они действительно просрочены относительно выбранной даты
- Группировка происходит относительно выбранной даты в селекторе
- Заголовок группы динамически меняется ("Сегодня" / "Завтра" / "Вчера" / "На эту дату")
- Логика показа разделов стала контекстуальной и понятной пользователю

## Измененные файлы
- `/app/frontend/src/components/TasksSection.jsx`:
  - Функция `groupTasksByDeadline()` (строки 340-388)
  - Функция `getTaskGroupTitle()` (строки 450-478)
  - Заголовок группы задач (строка 800)

## Статус
🟢 Исправление завершено. Frontend скомпилирован успешно.
