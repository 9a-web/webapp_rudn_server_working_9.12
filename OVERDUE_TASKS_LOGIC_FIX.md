# Упрощение логики показа задач - только на выбранную дату

## История изменений

### Версия 3 (Текущая) - Упрощенная логика
**Проблема v2:** Задачи с дедлайном на будущие даты появлялись в разделе "Позже", что создавало путаницу.

**Решение v3:** Реализован Вариант 1 - показываем **только задачи на выбранную дату**.

**Что показывается:**
- ✅ **На выбранную дату** - задачи с дедлайном строго на выбранную дату (00:00 - 23:59)
- ✅ **Без срока** - задачи без установленного дедлайна (показываются всегда)

**Что убрано:**
- ❌ Просроченные
- ❌ На этой неделе  
- ❌ Позже

### Версия 2 (Устаревшая) - Контекстуальная группировка
**Проблема v1:** Вкладка "Просроченные" появлялась при выборе всех дат в селекторе дней недели.

**Решение v2:** Группировка задач относительно выбранной даты (не текущего времени).

### Версия 1 (Устаревшая) - Относительно текущего времени
**Проблема:** Группировка всегда происходила относительно текущего времени (now).

## Текущая реализация (v3)

### Упрощенная логика фильтрации

Функция `groupTasksByDeadline()` теперь фильтрует задачи только для выбранной даты:

```javascript
const selectedDateStart = new Date(tasksSelectedDate);
selectedDateStart.setHours(0, 0, 0, 0);

const selectedDateEnd = new Date(tasksSelectedDate);
selectedDateEnd.setHours(23, 59, 59, 999);

const today = [];
const noDeadline = [];

filteredTasks.forEach(task => {
  if (!task.deadline) {
    noDeadline.push(task);
    return;
  }
  
  const deadline = new Date(task.deadline);
  
  // Показываем только задачи с дедлайном на выбранную дату
  if (deadline >= selectedDateStart && deadline <= selectedDateEnd) {
    today.push(task);
  }
});

return { today, noDeadline };
```

### Правила отображения:

1. **На выбранную дату (today)**:
   - Показываются только задачи с дедлайном строго на выбранную дату
   - `deadline >= selectedDateStart && deadline <= selectedDateEnd`
   - Заголовок меняется динамически: "Сегодня" / "Завтра" / "Вчера" / "На эту дату"

2. **Без срока (noDeadline)**:
   - Задачи без установленного дедлайна
   - Показываются всегда, независимо от выбранной даты

## Дополнительные улучшения

### Динамический заголовок группы
Добавлена функция `getTaskGroupTitle()`, которая определяет заголовок группы в зависимости от выбранной даты:

```javascript
const getTaskGroupTitle = () => {
  const selected = new Date(tasksSelectedDate);
  const today = new Date();
  
  if (selected === today) return 'Сегодня';
  if (selected === today + 1 day) return 'Завтра';
  if (selected === today - 1 day) return 'Вчера';
  
  return 'На эту дату'; // Для любой другой даты
}
```

Теперь в списке задач вместо статичного "Сегодня" отображается:
- "Сегодня" - если выбрана сегодняшняя дата
- "Завтра" - если выбрана завтрашняя дата
- "Вчера" - если выбрана вчерашняя дата
- "На эту дату" - для любой другой даты

## Результаты

### ❌ Было (v1-v2):
- Множество разделов: Просроченные, На выбранную дату, На этой неделе, Позже, Без срока
- Задачи с будущими дедлайнами показывались в разделе "Позже"
- Сложная логика с учетом текущего времени и временных интервалов
- Путаница при выборе разных дат

### ✅ Стало (v3):
- **Только 2 раздела**: "На выбранную дату" и "Без срока"
- Показываются **только задачи с дедлайном на выбранную дату**
- Простая и понятная логика
- Заголовок группы динамически меняется: "Сегодня" / "Завтра" / "Вчера" / "На эту дату"
- Задачи без дедлайна показываются всегда для удобства

### Преимущества упрощенной логики:
✅ Нет путаницы - пользователь видит только задачи на выбранную дату  
✅ Простота - не нужно разбираться в множестве разделов  
✅ Фокус - концентрация на конкретном дне  
✅ Удобство - задачи "Без срока" всегда доступны

## Измененные файлы
- `/app/frontend/src/components/TasksSection.jsx`:
  - Функция `groupTasksByDeadline()` (строки 340-388)
  - Функция `getTaskGroupTitle()` (строки 450-478)
  - Заголовок группы задач (строка 800)

## Статус
🟢 Исправление завершено. Frontend скомпилирован успешно.
