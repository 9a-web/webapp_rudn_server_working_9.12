# Команда /clear_db - Очистка базы данных

## 📋 Описание

Команда `/clear_db` позволяет полностью очистить базу данных приложения RUDN Schedule. Это критическая операция, доступная только администраторам с паролем.

## 🔐 Безопасность

### Уровни защиты:

1. **Проверка Admin ID** - команда доступна только пользователю с `telegram_id: 765963392`
2. **Проверка пароля** - требуется правильный пароль для выполнения операции
3. **Логирование** - все попытки использования команды записываются в лог

### Ограничения доступа:

- ✅ Только администраторы (ID: 765963392, 1311283832)
- ✅ Только с правильным паролем
- ❌ Другие пользователи получат сообщение "У вас нет прав"

## 📝 Использование

### Шаг 1: Запрос информации

Отправьте команду без параметров:

```
/clear_db
```

**Ответ бота:**
```
⚠️ ВНИМАНИЕ: Опасная операция!

Команда /clear_db очистит ВСЮ базу данных.

Для подтверждения используйте:
/clear_db <пароль>

🔐 Пароль должен быть известен только администратору.
```

### Шаг 2: Подтверждение с паролем

Отправьте команду с паролем:

```
/clear_db RUDN_CLEAR_2025
```

**Если пароль правильный:**
```
⏳ Начинаю очистку базы данных...

🗑 База данных очищена!

Всего удалено: XXX документов

Детали по коллекциям:
  • user_settings: XX
  • user_stats: XX
  • user_achievements: XX
  • tasks: XX
  • rooms: XX
  • group_tasks: XX
  • group_task_invites: XX
  • group_task_comments: XX
  • schedule_cache: XX
  • sent_notifications: XX
  • status_checks: XX
```

**Если пароль неправильный:**
```
❌ Неверный пароль!

Доступ запрещён.
```

## 🗄 Очищаемые коллекции

Команда очищает следующие коллекции MongoDB:

1. **user_settings** - настройки пользователей
2. **user_stats** - статистика пользователей
3. **user_achievements** - достижения пользователей
4. **tasks** - задачи пользователей
5. **rooms** - комнаты для совместных задач
6. **group_tasks** - групповые задачи
7. **group_task_invites** - приглашения в группы
8. **group_task_comments** - комментарии к групповым задачам
9. **schedule_cache** - кэш расписания
10. **sent_notifications** - отправленные уведомления
11. **status_checks** - проверки статуса

## ⚙️ Технические детали

### Конфигурация

Пароль хранится в файле `.env`:

```env
DB_CLEAR_PASSWORD="RUDN_CLEAR_2025"
```

Admin ID задан в коде:

```python
ADMIN_IDS = [765963392]
```

### Реализация

**Файл:** `/app/backend/telegram_bot.py`

**Функция:** `clear_db_command(update, context)`

**Алгоритм:**
1. Проверка telegram_id пользователя
2. Проверка наличия пароля в аргументах
3. Сравнение пароля с DB_CLEAR_PASSWORD
4. Итерация по всем коллекциям
5. Выполнение `delete_many({})` для каждой коллекции
6. Формирование отчёта с количеством удалённых документов
7. Отправка отчёта администратору

### Логирование

Все операции логируются:

```python
logger.warning(f"🚨 КРИТИЧЕСКАЯ ОПЕРАЦИЯ: Администратор {telegram_id} (@{username}) запросил очистку БД")
logger.warning(f"🚨 БАЗА ДАННЫХ ОЧИЩЕНА администратором {telegram_id} (@{username})")
logger.warning(f"📊 Удалено {total_deleted} документов из {len(collections)} коллекций")
```

## ⚠️ Важные замечания

1. **Необратимая операция** - восстановление данных невозможно без бэкапа
2. **Все данные удаляются** - включая пользователей, задачи, достижения
3. **Немедленное выполнение** - нет дополнительных подтверждений
4. **Детальный отчёт** - показывает количество удалённых документов по каждой коллекции

## 🔄 Альтернативные варианты

Если нужно очистить только определённые коллекции, можно:

1. Подключиться к MongoDB напрямую
2. Использовать MongoDB Compass
3. Создать дополнительную команду для частичной очистки

## 📞 Контакты

Владелец приложения: @shkarol (telegram_id: 765963392)

## 🔧 Изменение пароля

Чтобы изменить пароль, обновите значение в `/app/backend/.env`:

```env
DB_CLEAR_PASSWORD="ВАШ_НОВЫЙ_ПАРОЛЬ"
```

После изменения перезапустите backend:

```bash
sudo supervisorctl restart backend
```

## 📊 Статистика

После очистки база данных будет полностью пустой:
- 0 пользователей
- 0 задач
- 0 достижений
- 0 комнат
- Чистый кэш

Приложение продолжит работать, но все данные будут потеряны.
